{
  "name": "@plugola/mono-repo",
  "private": true,
  "version": "0.0.0",
  "description": "The MOL Ads mono-repo",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:generator": "yarn workspace @plugola/generator run build",
    "clean": "yarn workspaces foreach run clean",
    "commit": "yarn git-cz",
    "package:generate": "node --require ts-node/register $(yarn bin yo) ./packages/generator/src/package-generate",
    "package:remove": "node --require ts-node/register $(yarn bin yo) ./packages/generator/src/package-remove",
    "prepare": "husky install",
    "release": "yarn workspaces foreach --interlaced --no-private --topological-dev run release",
    "start": "yarn build && yarn workspaces foreach --interlaced --exclude @plugola/generator --parallel --interlaced run start"
  },
  "author": "",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/johngeorgewright/ts-mono-repo/issues"
  },
  "homepage": "https://github.com/johngeorgewright/ts-mono-repo#readme",
  "devDependencies": {
    "@commitlint/cli": "17.4.2",
    "@commitlint/config-conventional": "17.4.2",
    "@semantic-release/commit-analyzer": "9.0.2",
    "@semantic-release/exec": "6.0.3",
    "@semantic-release/git": "10.0.1",
    "@semantic-release/github": "8.0.7",
    "@semantic-release/release-notes-generator": "10.0.3",
    "commitizen": "4.3.0",
    "cz-conventional-changelog": "3.3.0",
    "husky": "8.0.3",
    "isbinaryfile": "5.0.0",
    "jest-environment-jsdom": "29.4.2",
    "json-parse-even-better-errors": "3.0.0",
    "lint-staged": "13.1.1",
    "mkdirp-infer-owner": "2.0.0",
    "prettier": "2.8.4",
    "semantic-release": "19.0.5",
    "semantic-release-monorepo": "7.0.5",
    "typescript": "4.9.5",
    "yo": "4.3.1"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "lint-staged": {
    "*.{md,json,js,jsx,ts,tsx,yml,yaml}": [
      "yarn prettier --write"
    ]
  },
  "dependencies": {
    "tslib": "2.5.0"
  },
  "packageManager": "yarn@3.4.1"
}
