{
  "name": "@plugola/mono-repo",
  "private": true,
  "version": "0.0.0",
  "description": "The MOL Ads mono-repo",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --interlaced --topological-dev --exclude @plugola/generator run build",
    "build:generator": "yarn workspace @plugola/generator run build",
    "clean": "yarn workspaces foreach run clean",
    "commit": "yarn git-cz",
    "generate:package": "yarn yo ./packages/generator/generators/package",
    "release": "yarn workspaces foreach --interlaced --no-private --topological-dev run release",
    "start": "yarn build && yarn workspaces foreach --interlaced --exclude @plugola/generator --parallel --interlaced run start",
    "test": "yarn workspaces foreach --interlaced run test"
  },
  "author": "",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/johngeorgewright/ts-mono-repo/issues"
  },
  "homepage": "https://github.com/johngeorgewright/ts-mono-repo#readme",
  "devDependencies": {
    "@commitlint/cli": "12.1.4",
    "@commitlint/config-conventional": "12.1.4",
    "@semantic-release/commit-analyzer": "8.0.1",
    "@semantic-release/git": "9.0.0",
    "@semantic-release/github": "7.2.3",
    "@semantic-release/npm": "7.1.3",
    "@semantic-release/release-notes-generator": "9.0.3",
    "commitizen": "4.2.4",
    "cz-conventional-changelog": "3.3.0",
    "husky": "7.0.1",
    "lint-staged": "11.1.1",
    "prettier": "2.3.2",
    "semantic-release": "17.4.4",
    "semantic-release-monorepo": "7.0.5",
    "typescript": "4.3.5",
    "yo": "4.3.0"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "lint-staged": {
    "*.{md,json,js,jsx,ts,tsx,yml,yaml}": [
      "yarn prettier --write"
    ]
  },
  "dependencies": {
    "tslib": "2.3.0"
  }
}
